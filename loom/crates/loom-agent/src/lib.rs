//! Agent backend implementations for Loom.
//!
//! Holds the pi-mono RPC and Claude Code stream-json backend implementations
//! of the `AgentBackend` trait declared in `loom-core`. Subsequent issues
//! populate each backend module; this crate currently exposes the skeleton
//! only.
